

pipeline {
  agent any
    docker {
      image "maven:3.8.6-jdk-11"
      lable "docker"
    }
  
  stages {
    stage("Build") {
      steps {
        sh "mvn -version"
        sh "mvn clean install"
      }
    }
  }
  
  post {
    always {
      cleanWs()
    }
  }
}  
  
  
   
