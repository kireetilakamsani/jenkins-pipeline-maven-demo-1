pipeline {

  agent any
  tools {
    maven "3.8.6"
  }  

  stages {
    stage("BUILD") {
      steps {
        sh "mvn -version"
        sh "mvn clean install"
      }
    }
  }


  post {
    always {
      cleanWs()
    }
  }
}
