pipeline {

  agent any
  tools {
    maven 'Maven'
  }  

  stages {
    stage("BUILD") {
      steps {
        sh "mvn -version"
      }
    }
  }


  post {
    always {
      cleanWs()
    }
  }
}
