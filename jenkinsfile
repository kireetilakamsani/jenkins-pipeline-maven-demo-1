pipeline {

  agent any
  tools {
    maven 'mymaven'
  }  

  stages {
    stage("BUILD") {
      steps {
        sh "mvn -version"
      }
    }
  }


  post {
    always {
      cleanWs()
    }
  }
}
