pipeline {

  agent any
  tools {
    mymaven "3.8.6"
  }  

  stages {
    stage("BUILD") {
      steps {
        sh "mvn -version"
        sh "mvn clean install"
      }
    }
  }


  post {
    always {
      cleanWs()
    }
  }
}
